# ArchScale Studio Hub Login

A production-ready React/Vite login screen for ArchScale Studio Hub, an architecture and interior design studio workspace.

## Scripts

- `npm run dev` starts the local development server.
- `npm run build` creates production files in `dist`.
- `npm run preview` serves the production build locally.

Demo credentials:

- Email: `a`
- Password: `a`
